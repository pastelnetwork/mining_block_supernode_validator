{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Start FastAPI Server",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "python": "${workspaceFolder}/venv/bin/python"
        }
    ]
}
